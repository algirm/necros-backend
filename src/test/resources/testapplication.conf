ktor {
    environment = dev
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ app.necros.ApplicationKt.module ]
    }
}

database {
    driver = "org.h2.Driver"
    url = "jdbc:h2:file:./build/db"
    user = "test"
    password = "test123"
}